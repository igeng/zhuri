"""zhuri (逐日) — a framework-free multi-agent orchestrator.

Implements the Deli_AutoResearch protocol: a terminal-first orchestrator that
spawns isolated agent OS processes and persists all state to files. No agent
framework is used; the LLM provider layer is called directly.
"""

__version__ = "0.1.0"
